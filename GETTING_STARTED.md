# 🆕 从零开始一个新故事 · 傻瓜式 Check 清单（v2）

> **适用**：刚装好工作台（`install.sh` 显示「✅ 安装完成」）
> **用时**：约 1 小时（不含等模型/等生成）
> **做法**：照下面一步步打勾，卡住了跑 `./doctor.sh` 或看文末「卡点速查表」

---

## 第 0 步｜一键体检（2 分钟，先确认能跑）

- [ ] 打开「终端」，执行：
  ```bash
  cd ~/novel-project && ./doctor.sh
  ```
- [ ] 看报告：**没有红色 ✗**（黄色 ⚠ 不影响写章）→ 环境就绪
- [ ] 有红色 ✗ → 按报告里的修复建议处理，或直接重跑 `install.sh` 自动修复

> doctor.sh 会检查：配置 → 模型服务 → 模型是否可路由 → 推演引擎 → 项目结构，
> 并直接告诉你怎么修。这是你之后所有卡点的第一求助对象。

---

## 第 1 步｜一键建新故事（2 分钟，不用手动填模板）

- [ ] 执行：
  ```bash
  cd ~/novel-project && ./new_story.sh
  ```
- [ ] 依次回答：书名 → 一句话故事 → 题材/基调 → 主角名（想跳过就回车，后面再填）
- [ ] 完成提示「新故事《你的书名》已就绪！」

系统已自动生成 3 个设定文件（含你的书名/主角，留了几个【待补充】占位）：
`memory/story_bible.yaml`（世界观）、`memory/characters.yaml`（人物）、`outlines/book_outline.yaml`（大纲）。

> 旧故事会被自动备份到 `outputs/backups/story-时间戳`，不会丢。

---

## 第 2 步｜让 AI 导演补充细节（10 分钟，推荐）

`new_story.sh` 只建了骨架，细节交给 AI 导演最省事：

1. 打开豆包（或 Codex），新建对话
2. 把 `~/novel-project/AGENTS.md` 的**全文**粘进去（教它怎么当导演）
3. 打一句话：
   > 我是《**你的书名**》的作者。请帮我细化设定：
   > 把 memory/ 和 outlines/ 里的【待补充】都填上，并给我一份计划，
   > 我先确认，你再动手。

4. AI 会给计划 → 你确认 → 它自动填好设定

- [ ] AI 给出了计划
- [ ] 你确认了计划
- [ ] 3 个设定文件里不再有【待补充】（或你觉得够用了）

> 想自己填？打开文件（`open memory/story_bible.yaml`）照注释改就行，更简单。

---

## 第 3 步｜写第一章（15 分钟 + 等 3-8 分钟生成）

- [ ] 终端执行（一句话 idea 换成你的）：
  ```bash
  cd ~/novel-project
  ./write_chapter.sh ch001-标题 "主角在第一幕最重要的一场戏一句话" 3000
  ```
- [ ] 等 3-8 分钟（本地模型写作，终端会显示：生成 → 补足字数 → 审查 → 修订 → 更新记忆）
- [ ] 检查成果：
  ```bash
  open chapters/ch001-标题/chapter.md
  ```
  → 正文约 3000 字、标题在最上方
- [ ] 桌面「小说章节」文件夹里出现 `ch001-标题.md`
- [ ] （可选）装了 Sodarie Novel 的话，打开它第一章已同步

---

## 第 4 步｜剧情卡住？让角色自己演（可选）

不知道下一章写什么时，别硬想——让角色自主推演：

- [ ] `./novel_flow.sh`
  → 引擎推演 6 轮 → 自动提炼一个分镜 idea → **停下给你审**
- [ ] 看 `outputs/drama/` 下的「故事全录」，挑有戏的片段
- [ ] 觉得 idea 可以 → 确认 → 成章

想全自动一条龙：
- [ ] `./novel_flow.sh --go`（推演 → 提炼 → 直接成章）

---

## 第 5 步｜日常写章节 · 每章 1 分钟检查

| 检查项 | 达标 |
|---|---|
| 字数 | ≈ 3000 字 |
| 标题 | `chXXX-标题` 与内容对应 |
| 正文首行 | 有标题（写手自动加的） |
| 记忆 | `memory/` 自动更新（AI 自动做） |
| 伏笔 | 新伏笔登记进 `memory/foreshadowing.yaml` |
| 看板 | `charts/` 自动刷新（可选组件，没装会提示跳过，不影响） |

---

## 🆘 卡点速查表

| 现象 | 先做什么 | 解决 |
|---|---|---|
| 任何报错/异常 | `./doctor.sh` | 按红色项修复建议处理 |
| `doctor.sh` 报模型服务离线 | 启动模型服务 | macOS 芯片打开 oMLX 应用；其他系统 `ollama serve` |
| `doctor.sh` 报模型未就绪 | 下载模型 | macOS：oMLX 里搜 `Qwen3.6-35B-A3B-MLX-8bit`；其他：`ollama pull lmstudio-community/Qwen3.6-35B-A3B-GGUF` |
| 生成的字数不够 3000 | 正常，写手会自动续写 | 多等一会儿，看终端进度 |
| 电脑发热 / 风扇响 | 35B 模型全力跑，正常 | 写完一章会降温；内存 <64GB 建议换小模型 |
| 想开新故事 | `./new_story.sh` | 问答几句自动建新骨架，旧故事在 outputs/backups/ |
| 改错/想恢复 | 看备份 | `outputs/backups/` 里有历史备份，拷回来即可 |
| 报错看不懂 | 截图 | 把终端报错截图给 AI 助手，它能帮你诊断 |

---

*照这个清单走一遍，第一本书就跑起来了。之后就是：想新剧情 → AI 出计划 → 你确认 → 成章，循环。*
