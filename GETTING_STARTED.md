# 🆕 从零开始一个新故事 · 傻瓜式 Check 清单

> **适用**：刚装好工作台（`install.sh` 显示「✅ 安装完成」）
> **用时**：约 1 小时（不含等模型/等生成）
> **做法**：照下面一步步打勾，卡住了看文末「卡点速查表」

---

## 第 0 步｜确认能跑（5 分钟）

- [ ] 打开「终端」，执行：
  ```bash
  cd ~/novel-project && ./nest_drama_run.sh status
  ```
  → 看到引擎在线 / 模型就绪 的字样，说明环境和模型都好了
- [ ] 桌面出现「小说章节」文件夹
- [ ] （可选）装好 Sodarie Novel，章节生成后打开它就能看小说

> 如果 status 报错：先打开模型服务（Mac 芯片的 oMLX 应用 / 其他系统的 Ollama），确认两个模型都下载完成，再重试。

---

## 第 1 步｜告诉 AI 你想写什么（10 分钟）

**这是最省心的一步——让 AI 当导演，你当老板。**

1. 打开豆包（或 Codex），新建对话
2. 把 `~/novel-project/AGENTS.md` 的**全文**粘进去（它告诉 AI 怎么当导演）
3. 然后打一句话：
   > 你是这部小说的导演。我要写一个「**你的故事一句话**」。
   > 请先给我一份计划：世界观、主要人物、全书大纲。
   > 我先确认，你再动手。

4. AI 会给出计划（世界观 / 人物 / 大纲）

- [ ] AI 给出了完整计划
- [ ] 你确认了计划（不满意就让 AI 改，直到你点头）

---

## 第 2 步｜建立记忆（AI 帮你做 / 或手动填 3 个模板）

> AI 确认计划后，会按 AGENTS.md 自动帮你填下面 3 个文件。
> 如果你想自己填，就打开文件照模板里的注释写。

- [ ] `memory/story_bible.yaml` —— 故事圣经
      标题、基调、世界规则（至少 3 条）
- [ ] `memory/characters.yaml` —— 人物档案
      至少 3 个角色（主角、对手、一个关系人）
- [ ] `outlines/book_outline.yaml` —— 全书大纲
      一句话故事、三幕结构、每幕写什么

**完成标准**：这 3 个文件不再是"示例"内容，填的是你自己的故事。

---

## 第 3 步｜写第一章（15 分钟 + 等 3-8 分钟生成）

- [ ] 终端执行（一句话 idea 换成你的）：
  ```bash
  cd ~/novel-project
  ./write_chapter.sh ch001-标题 "主角在第一幕最重要的一场戏一句话" 3000
  ```
- [ ] 等 3-8 分钟（本地模型写作，期间终端会显示：生成 → 补足字数 → 审查 → 修订 → 更新记忆）
- [ ] 检查成果：
  ```bash
  open chapters/ch001-标题/chapter.md
  ```
  → 正文约 3000 字、标题在最上方
- [ ] 桌面「小说章节」文件夹里出现 `ch001-标题.md`
- [ ] （可选）打开 Sodarie Novel，第一章已自动同步进去

---

## 第 4 步｜剧情卡住？让角色自己演（可选）

不知道下一章写什么时，别硬想——让角色自主推演：

- [ ] `./novel_flow.sh`
  → 引擎推演 6 轮 → 自动提炼一个分镜 idea → **停下给你审**
- [ ] 看 `outputs/drama/` 下的「故事全录」，挑有戏的片段
- [ ] 觉得 idea 可以 → 确认 → 成章

想全自动一条龙：
- [ ] `./novel_flow.sh --go`（推演 → 提炼 → 直接成章）

---

## 第 5 步｜日常写章节 · 每章 1 分钟检查

每写完一章，快速扫一眼：

| 检查项 | 达标 |
|---|---|
| 字数 | ≈ 3000 字 |
| 标题 | `chXXX-标题` 与内容对应 |
| 正文首行 | 有标题（写手自动加的） |
| 记忆 | `memory/` 自动更新（AI 自动做） |
| 伏笔 | 新伏笔登记进 `memory/foreshadowing.yaml` |
| 看板 | `charts/` 自动刷新（可选打开看） |

---

## 🆘 卡点速查表

| 现象 | 原因 | 解决 |
|---|---|---|
| `install.sh` 卡在下载模型 | 网络慢 | 中断后跑 `bash install.sh --skip-models`，用 oMLX 应用图形界面下载 |
| `./nest_drama_run.sh status` 报错 | 模型服务没开 / 模型没下完 | 打开模型服务（Mac 芯片的 oMLX / 其他系统的 Ollama），等模型就绪后重试 |
| `write_chapter.sh` 报"找不到模型" | 写作模型未下载 | Mac 芯片：oMLX 里搜 `Qwen3.6-35B-A3B-MLX-8bit`；其他系统：`ollama pull lmstudio-community/Qwen3.6-35B-A3B-GGUF` |
| 生成的字数不够 3000 | 正常，写手会自动续写 | 多等一会儿，看终端进度 |
| 电脑发热 / 风扇响 | 35B 模型在全力跑，正常 | 写完一章会降温；内存 <64GB 建议换小模型 |
| 想换一个故事重写 | 想开新书 | 清空 `memory/` 和 `outlines/`（照模板重填），或复制 `templates/project` 新建项目 |
| 报错看不懂 | 终端报错 | 截图给 AI 助手，它能帮你诊断 |

---

*照这个清单走一遍，第一本书就跑起来了。之后就是：想新剧情 → AI 出计划 → 你确认 → 成章，循环。*
